import { ShellRenderDirective } from './shell-render.directive';

describe('ShellRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellRenderDirective();
    expect(directive).toBeTruthy();
  });
});
